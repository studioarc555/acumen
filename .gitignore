# See https://help.github.com/articles/ignoring-files/ for more about ignoring files.

# dependencies
/node_modules
/.pnp
.pnp.*
.yarn/*
!.yarn/patches
!.yarn/plugins
!.yarn/releases
!.yarn/versions

# testing
/coverage

# next.js
/.next/
/out/

# production
/build

# misc
.DS_Store
*.pem

# debug
npm-debug.log*
yarn-debug.log*
yarn-error.log*
.pnpm-debug.log*

# env files (can opt-in for committing if needed)
.env*

# vercel
.vercel

# typescript
*.tsbuildinfo
next-env.d.ts
# Common IDE/OS files
.vscode/
.idea/
.DS_Store

# Gradle/Java Specific Files
# Ignore the build output directory
build/

# Ignore caches/temp files (these are the ones showing up in your screenshot)
# They are usually temporary files created in the project directory, 
# but since the path in your screenshot shows C:\Users\shahb\.gradle\caches, 
# we need a global ignore or ensure no build is running.
# If the files are appearing *inside* your project's repository, 
# add the pattern for the directory they are in.

# If using Android Studio/Gradle:
# Ignore ALL files in the user's Gradle cache directory
C:/Users/shahb/.gradle/caches/
.gradle/
local.properties
*.iml
# clerk configuration (can include secrets)
/.clerk/
